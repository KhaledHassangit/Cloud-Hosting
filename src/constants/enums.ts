export enum Routes {
    ROOT = "/",
    ARTICLES = "articles",
    ABOUT = "about",
    ADMIN = "admin",
    profile = "profile",
}

export enum Pages {
    LOGIN = "login",
    Register = "register",
}

export enum Environments {
    PROD = "production",
    DEV = "development",
  }
  
export enum DOMAIN {
    LOCALHOST = "http://localhost:3000"
}

export const pageSize = 6;
